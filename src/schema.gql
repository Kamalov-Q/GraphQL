# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Achievement {
  id: String!
  title: String!
  description: String
  points: Int!
  difficulty: Difficulty!
}

"""Difficulty levels for achievements"""
enum Difficulty {
  EASY
  MEDIUM
  HARD
}

type Game {
  id: String!
  name: String!
  genre: String!
  achievements(offset: Int!, limit: Int!, difficulty: Difficulty): [Achievement!]!
}

type Query {
  getGames(offset: Int!, limit: Int!): [Game!]!
  getSingleGame(id: String!): Game!
}

type Mutation {
  createGame(input: CreateGameInput!): Game!
  createAchievements(input: CreateAchievementInput!): Achievement!
}

input CreateGameInput {
  name: String!
  genre: String!
}

input CreateAchievementInput {
  gameId: String!
  title: String!
  description: String!
  difficulty: Difficulty!
  points: Int!
}